{
  "name": "@siteprobe/scanner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "install-browser": "playwright install chromium",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@siteprobe/contracts": "workspace:*",
    "playwright": "1.62.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
